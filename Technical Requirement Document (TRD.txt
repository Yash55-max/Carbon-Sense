Technical Requirement Document (TRD)Markdown# Technical Requirement Document (TRD): CarbonSense

## 1. Technical Stack & File Structure
The application will use a clean, modular React component architecture powered by Tailwind CSS and Lucide React icons to fulfill **Code Quality** and **Efficiency** standards.

```text
src/
├── components/
│   ├── shared/
│   │   ├── Sidebar.jsx        # Sticky navigation console with Sync API trigger
│   │   └── MetricCard.jsx     # Reusable tech-styled data readout component
│   ├── dashboard/
│   │   ├── EmissionChart.jsx  # SVG or Chart.js line graph for timeline tracking
│   │   └── StreakTracker.jsx  # Daily micro-habit checklist module
│   ├── log/
│   │   └── ActivityWizard.jsx # Multi-step slider and vehicle type registry
│   └── insights/
│       ├── EquivalencyGrid.jsx# Concrete environmental translation blocks
│       └── SmartRecs.jsx      # Impact cards with action vectors
├── context/
│   └── CarbonProtocolContext.jsx # Centralized state pipeline for scores & inputs
├── App.jsx                     # Route definitions and workspace wrapper
└── main.jsx
2. Evaluation Parameter Implementations1. Code Quality (High Impact Execution)Requirement: Enforce declarative, clean state management. Avoid deeply nested components passing props downward.Implementation: Implement a central React Context (CarbonProtocolContext) handling global data structures:JavaScriptconst initialProtocolState = {
  monthlyFootprint: 420,
  streakDays: 14,
  activities: { transit: 42, energy: 185, food: 68, shopping: 25 },
  unlockedBadges: ['EARLY_ADOPTER', 'CARBON_SINK']
};
2. Security (Medium Impact Execution)Requirement: Safe and defensive client-side implementation preventing execution injection or state corruption.Implementation:Strictly sanitize numeric inputs received from the /log-activity range sliders.Enforce type compliance via explicit parsing: const validatedDistance = Math.min(Math.max(0, parseFloat(inputValue)), 500);.Ensure all external links used in the application layout or documentation utilize rel="noopener noreferrer".3. Efficiency (Medium Impact Execution)Requirement: Light, efficient resource handling with no redundant computational layouts.Implementation:Memoize heavy computational summaries (like calculation engine formulas mapping transit distances to metric tons) using React's useMemo to prevent calculation recalculations on unrelated state updates.Use highly performant utility arrays for rendering elements dynamically instead of duplicating card layouts manually across the Dashboard and Insights pages.4. Testing (Medium Impact Execution)Requirement: Verifiable operational states across all interactive nodes.Implementation: Build modular verification configurations:Unit Test Target: Verify the mathematical conversion logic inside the Equivalency Engine accurately translates $X\ kg\ \text{CO}_2\text{e}$ to corresponding tree/smartphone metrics.Integration Test Target: Verify that shifting the slider from 0 to 42 KM dynamically triggers a corresponding update in the "Real-Time Impact" preview block instantly.5. Accessibility / WCAG (Low Impact Execution)Requirement: Fully inclusive execution inside a dark mode theme wrapper.Implementation:Maintain strict contrast compliance. Ensure neon highlight strings (#10B981) sit cleanly on contrasting slate backgrounds (#0B0F19) or wrap them within high-contrast borders.Provide explicit semantic HTML landmarks (<nav>, <main>, <aside>, <section>).Equip all slider inputs and habit checkbox elements with comprehensive aria-label definitions (e.g., aria-label="Log daily transit distance in kilometers").